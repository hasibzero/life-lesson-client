export const metadata = {
  title: "Explore Lessons | Digital Life Lessons",
  description: "Explore public lessons shared by others.",
};

export default function LessonsLayout({ children }) {
  return children;
}
