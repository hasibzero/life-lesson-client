export const metadata = {
  title: "Author Profile | Digital Life Lessons",
  description: "View published lessons, guides, and stories shared by this author.",
};

export default function ProfileLayout({ children }) {
  return children;
}
