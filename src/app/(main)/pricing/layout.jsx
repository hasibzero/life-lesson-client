export const metadata = {
  title: "Pricing | Digital Life Lessons",
  description: "Upgrade your account to access premium features.",
};

export default function PricingLayout({ children }) {
  return children;
}
