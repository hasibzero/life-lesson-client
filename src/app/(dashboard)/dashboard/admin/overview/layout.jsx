export const metadata = {
  title: "Admin Overview | Digital Life Lessons",
  description: "Real-time platform vitals and analytics overview.",
};

export default function AdminOverviewLayout({ children }) {
  return children;
}
