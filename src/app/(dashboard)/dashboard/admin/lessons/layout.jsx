export const metadata = {
  title: "Admin Lessons | Digital Life Lessons",
  description: "View and manage admin lessons.",
};

export default function AdminLessonsLayout({ children }) {
  return children;
}
