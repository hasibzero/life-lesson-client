export const metadata = {
  title: "Reported Content | Digital Life Lessons",
  description: "Manage reported content.",
};

export default function ReportedContentLayout({ children }) {
  return children;
}
