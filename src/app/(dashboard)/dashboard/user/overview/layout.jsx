export const metadata = {
  title: "Dashboard Overview | Digital Life Lessons",
  description: "View your dashboard overview.",
};

export default function UserOverviewLayout({ children }) {
  return children;
}
