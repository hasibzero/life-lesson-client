export const metadata = {
  title: "Add Lesson | Digital Life Lessons",
  description: "Create and publish new curated wisdom for the platform.",
};

export default function AddLessonLayout({ children }) {
  return children;
}
